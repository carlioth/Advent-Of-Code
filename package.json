{
  "name": "adventofcode",
  "version": "1.0.0",
  "description": "typescript for advent of code",
  "main": "index.js",
  "scripts": {
    "start": "npm run clean && npm run compile && npm run assets && node ./dist/Day1/Day1.js",
    "compile": "node ./node_modules/typescript/bin/tsc",
    "assets": "cp -R src/input/ dist/input/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist"
  },
  "author": "Henrik Carlioth",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^2.1.4",
    "rimraf": "latest",
    "@types/node": "^6.0.53"
  }
}